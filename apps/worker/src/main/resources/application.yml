spring:
  application:
    name: universal-doc-generator-worker

docgen:
  storage-path: ./storage
  worker:
    poll-interval-ms: 5000
    max-concurrent-jobs: 2

logging:
  level:
    root: INFO
    io.docgen: DEBUG
